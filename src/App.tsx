import React from 'react';
import GPGAManager from './AppOld.jsx'; // Import old app

/**
 * Temporary App wrapper
 * Just renders the old App.jsx for now
 */
const App: React.FC = () => {
  return <GPGAManager />;
};

export default App;
